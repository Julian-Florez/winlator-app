# Cuphead asset pack 01

This module intentionally does not contain the user's game data. Place the
locally generated `cuphead_data_01.part` file in `src/main/assets/` before a
Cuphead build. The source must come from a legally owned installation and is
ignored by Git because it is a large user payload.
